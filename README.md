# Constellation Canvas React App

## Setup

1. Install dependencies:

```bash
npm install
```

2. Start the development server:

```bash
npm run dev
```

The app will open automatically in your default browser and display the constellation canvas animation.

## Build

To build the app for production:

```bash
npm run build
```

To preview the production build locally:

```bash
npm run preview
